import styled from 'styled-components';

const Wrapper = styled.div`
  display: grid;
  grid-template-columns: 40% 60%;
`;
const WrapperImg = styled.div``;
const WrapperContent = styled.div``;

export { Wrapper, WrapperImg, WrapperContent };
